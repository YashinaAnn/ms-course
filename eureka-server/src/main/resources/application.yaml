spring:
  application:
    name: "eureka-server"
  security:
    user:
      name: "netflix"
      password: "eureka"

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
